let winkelmand =["bananen", "Wisky", "chips","bonen","aardappelen"];

let paragraaf = document.getElementById("para");

//paragraaf.innerText = "er zitten " + winkelmand.length + " artikelen in mijn winkelmand";

//paragraaf.innerText = winkelmand;

//paragraaf.innerText = winkelmand[3] + " staat op de vierde plek in uw winkelmand";

// winkelmand[1] ="bier";
 //paragraaf.innerText = winkelmand;

 //function product_toevoegen() {
   // winkelmand.push("bryan");
   // paragraaf.innerText = winkelmand;

  // if (winkelmand.length <=1){
   // paragraaf.innerText = "u heeft niet genoeg producten om te tonen"
  // } else {
   // paragraaf.innerText = winkelmand;
  // }

    if (winkelmand.length <=4){
    paragraaf.innerText = "Winkelmand"
   } else {
    paragraaf.innerText = "u heeft geen drop";
   }




